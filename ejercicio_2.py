"""Construir una función que reciba una cadena digitada (como parámetro) por el usuario 
y que lo muestre n veces en la pantalla, el valor de n también es digitado por el usuario"""


def Nombre_N_Veces(cadena):
    for i in range (n):
        print(cadena)

n = int(input("Digite el valor de n: "))
cadena = input("Ingrese una frase: ")
Nombre_N_Veces(cadena)